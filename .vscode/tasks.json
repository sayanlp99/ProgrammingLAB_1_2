{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc-6 build active file",
            "command": "/usr/bin/gcc-6",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}